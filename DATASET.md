# 실데이터 탐지 데이터셋 구성 (71858 → YOLO)

> 이 문서 하나로 **무엇을 어떻게 만들었는지 + 왜 그렇게 했는지 + 어떻게 재현하는지**가 닫히도록 썼습니다.
> 실행 명령만 필요하면 `VAST.md`, 겪은 함정은 `../스모크테스트_유의사항.md` §10·§14를 보세요.

---

## 1. 원본 데이터

**AI Hub 71858 「군 경계 작전 환경 내 인식 데이터」의 `3.개방데이터`** 만 사용했습니다. 전체 배포본(승인 필요분)이 아닙니다.

| 항목 | 값 |
|---|---|
| 클립 | 863개 (`I1`=연평도, `I2`=백령도) |
| 라벨 JSON | 41,972개 (프레임 1장당 1개) |
| 추출 이미지 | 41,718장 (**254장은 zip 손상으로 스킵** — 정상) |
| 해상도 | 1920×1080 |

### 🔴 개방데이터는 "쉬운 슬라이스"가 아니라 "좁은 슬라이스"입니다
전수 확인 결과 **camera 전부 C5(일반카메라), scenario 전부 1(이동), 시간대 전부 주간, 센서 전부 EO**입니다. spot(촬영지점)만 14종으로 다양합니다.

⇒ **야간·IR·고정CCTV 데이터가 0장**입니다. 이 제약이 이후 모든 판단의 전제가 됩니다(야간 성능을 실데이터로 검증할 수 없는 이유).

### 파일명 규칙
```
I2_S0_C5_0296001.jpg
└┬┘ └┬┘└┬┘ └──┬──┘
장소 지점 장비  클립4자리 + 프레임3자리   ← 구분자 없이 붙어 있음
```
**클립 ID = `stem[:-3]`**. 분할이 이 단위로 이뤄집니다.

---

## 2. 클래스

`meta.annotations[].class` 를 YOLO 인덱스로 매핑합니다.

| 원본 class | YOLO | 이름 | 비고 |
|---|---|---|---|
| `"0"` | 0 | `fishing_boat` 어선 | |
| `"1"` | 1 | `merchant_ship` 상선 | |
| `"2"` | 2 | `warship` 군함 | **탐지 목표(위협)** |
| `"3"` 사람 · `"4"` 복합 | — | **제외** | 박스를 버리고 무라벨 배경으로 둠 |

### 사람·조류를 왜 버리는가
탐지 목표가 선박이고, 사람·새를 클래스로 넣으면 평가 축이 흐려집니다. 버린 객체는 화면에 남지만 라벨이 없어 **배경(hard negative)으로 학습**됩니다 — 즉 "저건 배가 아니다"를 배우게 되므로 오히려 유익합니다.

> ⚠️ 이 규약은 이후 합성데이터(71856)를 섞을 때도 그대로 유지됩니다. 라벨 공간이 어긋나면 한쪽에서 라벨된 물체를 다른 쪽에선 배경으로 억누르도록 학습되기 때문입니다.

### bbox 변환
원본은 `bbox: [x, y, w, h]`(좌상단 + 폭/높이), 픽셀 단위입니다. YOLO 형식으로:
```
cx = (x + w/2) / W,  cy = (y + h/2) / H,  nw = w / W,  nh = h / H
```
빈 프레임(선박 0개)은 **빈 `.txt`** 로 저장합니다 — 삭제하지 않습니다. 배경 샘플이 탐지기에 유익합니다.

---

## 3. 🔴 분할 — 가장 중요한 부분

같은 데이터로 세 번 나눴고, **나눌수록 숫자가 정직해졌습니다.** 팀원이 이 부분만은 꼭 이해해야 합니다.

| 분할 | 파일 | train / val (클립) | train / val (프레임) | 격리하는 것 |
|---|---|---|---|---|
| ① clip | `splits/` | 691 / 172 | 34,088 / 7,884 | 클립만 |
| ② session | `splits_session/` | 680 / 183 | 32,839 / 8,879 | **(날짜, 지점) 세션** |
| ③ **spot** | `splits_session_spot/` | 593 / 270 | 28,698 / 13,020 | **지점 자체 = 새 장소** |

### 왜 ①로는 부족했나
클립 단위로 나누면 클립은 안 겹칩니다. 그런데 **같은 날 같은 지점에서 찍은 다른 클립**이 train과 val 양쪽에 들어갑니다(val 군함 세션의 83%가 train과 겹쳤습니다). 배경·조명·파도가 사실상 동일하니 모델이 "그 장면"을 외우면 val에서도 맞힙니다.

**증거는 절대값이 아니라 곡선이었습니다** — ① 분할에서는 **epoch 1에 이미 mAP50 0.977**이 나왔습니다. 1에폭 만에 val이 풀린다는 건 학습이 아니라 재인식입니다. ② 분할에서는 epoch 1이 0.913이고 best가 epoch 61이었습니다(점진 학습 = 정상 서명).

### 어느 걸 써야 하나
- **② session** = "같은 장소, 새로운 날" → **고정 설치 배포**의 정직한 값
- **③ spot** = "완전히 새로운 장소" → **새 지점 배치**의 정직한 값. 가장 보수적이라 **현재 기본값**입니다.

---

## 4. 결과 (참고용)

세 분할 모두 YOLO11s / imgsz 1280 / batch 16.

| 분할 | 군함 mAP50 | 어선 | 상선 | 전체 |
|---|---:|---:|---:|---:|
| ① clip | 0.964 | 0.980 | 0.994 | 0.980 |
| ② session | **0.982** (P0.993 R0.961) | 0.994 | 0.981 | 0.986 |
| ③ spot | **0.979** (P0.951 R0.975) | 0.992 | **0.913** | 0.961 |

**핵심:** 세 분할에서 군함이 0.96~0.98로 흔들리지 않습니다. ①의 0.98은 *가짜가 아니라 검증 절차가 허술했던 것*이고, 정직하게 다시 재도 결론이 유지됐습니다.

**주의할 점:** 상선은 새 장소에서 0.913으로 떨어집니다(recall 0.905). 지점 다양성이 부족한 소수 클래스입니다. 군함은 무관하게 견고합니다.

운영점 `conf=0.6`에서 군함 P0.993 / R0.960, 빈 프레임 오경보 0.2%(세션 분할 기준. **새 장소에서는 2.12%로 10배**입니다).

---

## 5. 재현 절차

```bash
# 0) 준비 — 라벨은 repo에 포함(labels_train.tgz), 이미지 zip은 Drive에서
mkdir -p labels_train && tar -xzf labels_train.tgz -C labels_train --strip-components=1
ls labels_train | wc -l          # 41972

rclone copy "gdrive:" . --drive-root-folder-id 1FE_dm5yE_OwgJun2cfJqamoqCV3Qw3AB \
    --include "**/TS_I*_S0_C5.zip" -P        # 이미지 29GB

# 1) zip -> 평면 jpg
python scripts/extract_images.py --zip-dir <TS_*.zip 있는 폴더> --out datasets/marine_frames
#    -> 41,718 프레임 / 손상 254 스킵 이면 정상

# 2) 라벨 -> YOLO + 분할 적용 (심링크라 디스크 절약)
python scripts/json_to_yolo.py \
    --labels-dir labels_train --images-dir datasets/marine_frames \
    --splits-dir splits_session_spot --out datasets/marine --link
#    -> [train] frames=28698 boxes=29990 / [val] frames=13020 boxes=14272
#    -> img_missing 합이 254(=손상수)면 정상. 0이 아니라 254가 정상입니다.

# 3) 학습
bash scripts/train.sh yolo11s.pt 1280 100 16
```

> 🔴 **분할 파일을 새로 만들지 마세요.** `splits*/`의 `.txt`는 repo에 커밋돼 있고, **그 파일을 그대로 써야 같은 벤치마크**입니다. `build_split*.py`로 재생성하면 클립 배정이 달라져 다른 val셋이 되고, 우리 숫자와 비교가 불가능해집니다. (새 실험 설계를 할 때만 재생성하세요.)

---

## 6. 다른 모델을 같은 벤치마크로 재기

### 6-1. 먼저 데이터셋이 동일한지 확인
`json_to_yolo.py` 출력이 **정확히 이 숫자**여야 합니다. 하나라도 다르면 벤치마크가 다른 것입니다.

```
[train] frames=28698  boxes=29990  img_missing=252
[val]   frames=13020  boxes=14272  img_missing=2
```
val 인스턴스 내역: 어선 7,919 · 상선 1,910 · 군함 4,443 (총 14,272)

### 6-2. 평가 설정 (반드시 고정)

| 항목 | 값 | 이유 |
|---|---|---|
| `imgsz` | **1280** | 군함이 native 1920에서 50~100px로 작음. 640으로 재면 크게 떨어져 비교 불가 |
| `conf` (mAP용) | **0.001** (ultralytics 기본) | 표준 mAP 정의. 높이면 잘린 값이 나옴 |
| `iou` (NMS) | 0.7 (기본) | |
| mAP 기준 | mAP50 = IoU 0.5, mAP50-95 = 0.5:0.95 | |
| val 데이터 | `splits_session_spot` 기준 13,020장 | 새 장소 일반화 |

```bash
yolo detect val model=<모델>.pt data=configs/marine.yaml imgsz=1280 plots=False
```

**운영점 지표는 따로 재세요.** mAP와 섞으면 안 됩니다:
```bash
yolo detect val model=<모델>.pt data=configs/marine.yaml imgsz=1280 conf=0.6
```
우리 기준값은 mAP50 0.979 / 운영점(conf 0.6) 군함 P0.951 R0.975 입니다.

### 6-3. ⚠️ 다른 프레임워크로 잰다면
위 숫자는 **ultralytics의 mAP 구현** 기준입니다. mmdetection·detectron2·COCO API는 보간 방식과 매칭 규칙이 미묘하게 달라 **같은 예측이라도 1~2%p 차이**가 날 수 있습니다.

프레임워크가 다르면 mAP 수치를 직접 비교하지 말고, 둘 중 하나를 하세요:
- 예측 결과를 COCO JSON으로 내보내 **같은 평가기**로 양쪽을 재기
- 또는 우리 `best_spot.pt`를 그쪽 평가기로도 돌려 **기준점을 같이 옮기기**

프레임워크가 같고(ultralytics) 위 설정을 지켰다면 그대로 비교 가능합니다.

---

## 7. 팀원이 빠지기 쉬운 함정

| | 내용 |
|---|---|
| **`img_missing=254`가 정상** | zip에 손상 항목이 ~1% 섞여 있습니다. 0을 기대하면 계속 디버깅하게 됩니다. |
| **심링크는 절대경로로** | 상대경로 심링크는 자기 위치 기준으로 풀려 깨집니다. `json_to_yolo.py --link`는 절대경로로 겁니다. |
| **`configs/marine.yaml`의 `path`** | 실행 호스트마다 다릅니다. vast 기준 `/workspace/YOLO-pipeline`. |
| **분할을 바꾸면 val 구성이 바뀝니다** | 분할이 다른 실험끼리 mAP를 직접 비교하면 안 됩니다. 비교하려면 같은 분할로 재평가하세요. |
| **ultralytics 출력 경로** | `project=runs`로 줘도 실제 저장은 `runs/detect/runs/<name>/`입니다. 스크립트에서 경로를 가정하지 마세요. |

---

## 8. 관련 파일

| 파일 | 내용 |
|---|---|
| `scripts/build_split.py` / `build_split_session.py` | 분할 생성 |
| `scripts/extract_images.py` | zip → 평면 jpg |
| `scripts/json_to_yolo.py` | AI Hub JSON → YOLO 라벨 |
| `configs/marine.yaml` | data config (3클래스) |
| `VAST.md` | vast 실행 런북 |
| `../W4_탐지파이프라인_결과정리.md` | ① 분할 결과와 배경 |
| `../W5_다음검증_시작점.md` | ②③ 재검증 상세 |
| `../스모크테스트_유의사항.md` §10·§14 | 데이터·전송 함정 |
